/**
 * 주소록 관리를 위한 mutation
 *
 * 들어가야 할 기능으로
 */
export default{
  /* 새로운 주소록 추가*/
  /**
   *
   * @param context
   * @param{Object} payload
   * @param{string} payload.id - address root id
   */
  createNewAddress(context,payload){
    // find item by id in context state vue

  }
}
